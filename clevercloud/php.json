{
    "deploy": {
      "command": "cd backend && composer install --no-dev --prefer-dist --no-interaction --optimize-autoloader && php -r \"if (!file_exists('/tmp/database.sqlite')) { touch('/tmp/database.sqlite'); }\" && php artisan optimize:clear && php artisan migrate --force --seed"
    }
}