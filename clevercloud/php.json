{
    "deploy": {
      "command": "cd backend && composer install --no-dev --prefer-dist --no-interaction --optimize-autoloader && php artisan optimize:clear && php artisan migrate --force --seed"
    }
}